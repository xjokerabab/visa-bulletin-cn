# 数据仓库：只跟踪数据本体与元数据，不跟踪任何本地/工具产物。
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
*.swp

# 依赖（本仓库无运行时依赖，仅防误提交）
node_modules/

# 工作区记忆与 AI 工具产物
#
# 这个仓库是公开的，本地工作记忆不属于数据集的一部分。ym-shuju 那边把
# .workbuddy-ai/memory 纳入版本控制是有意的（私有仓库、要跨会话续接），
# 这里不能照抄——同一条规则在公开仓库就是泄露。
.workbuddy-ai/
